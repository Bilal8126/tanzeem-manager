name = "tanzeem-manager"
main = "worker.js"
compatibility_date = "2025-01-01"

# Secrets (never commit real values — set via: npx wrangler secret put SECRET_NAME)
# GEMINI_API_KEY, DRIVE_CLIENT_ID, DRIVE_CLIENT_SECRET, DRIVE_REFRESH_TOKEN
